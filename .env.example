# API Keys
GEMINI_API_KEY=<your_api_key>
GROQ_API_KEY=<your_api_key>
ANTHROPIC_API_KEY=<your_api_key>

# Postgres (schema `dataload` — ver data/sql/schema.sql)
DATABASE_URI=postgresql://frigus:frigus@localhost:5432/frigus

# Mongo (histórico do chatbot + perfil comportamental + checkpoints do LangGraph)
MONGODB_URI=mongodb://localhost:27017

# ---------------------------------------------------------------------------
# Ainda NÃO existem em config/settings.py — entram junto com a feature que os
# usa (ver TODO.md). Listados aqui só como referência do env final planejado:
#
#   LANGSMITH_TRACING / LANGSMITH_API_KEY / LANGSMITH_PROJECT   (observabilidade)
#   REDIS_URL                                                   (cache de perfil)
#   QDRANT_URL / QDRANT_API_KEY / QDRANT_COLLECTION_NAME        (RAG do FAQ)
#
# Não adicionar ao Settings antes da tool correspondente existir — campo
# obrigatório sem uso quebra o import do projeto inteiro pra quem não tiver.
# ---------------------------------------------------------------------------
